## What changed

<!-- One or two sentences. Lead with the outcome, not the diff. -->

## Why

<!-- The problem this solves, or the issue it closes. -->

## Checks

- [ ] `bun run verify` passes locally
- [ ] Tests cover the change — for a bug fix, a test that fails without it

## If you added an export target

- [ ] `id` added to the `TargetId` union
- [ ] Output was pasted into a real project of that framework and works
- [ ] A `roundtrip.test.ts` case, if the target embeds JSON-LD

## If you touched the interface

- [ ] Keyboard reachable, with visible focus
- [ ] Any new colour is a validation signal, not decoration
